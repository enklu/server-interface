// AUTOGENERATED CODE: DO NOT EDIT

<% const categories = Object.entries(parsedCollection); -%>
// /////////////////////////////////////////////////////////////////////////////
// URIs
// /////////////////////////////////////////////////////////////////////////////
<% categories.forEach(([name, items], categoryIndex) => { %>
// <%= name %>
<% items.forEach(({ uri, action, func }, index) => { -%>
<%- `const ${uri};` %>
<% }) -%>
<% }) -%>

// /////////////////////////////////////////////////////////////////////////////
// ACTION TYPES
// /////////////////////////////////////////////////////////////////////////////
<% categories.forEach(([name, items], categoryIndex) => { %>
// <%= name %>
<% items.forEach(({ uri, action, func }, index) => { -%>
<%- `export const ${action};` %>
<% }) -%>
<% }) -%>

// /////////////////////////////////////////////////////////////////////////////
// ACTIONS
// /////////////////////////////////////////////////////////////////////////////
export const createActions = ({ getify, postify, putify, deletify }) => ({
<% categories.forEach(([name, items], categoryIndex) => { %>
    // <%= name %>
<% items.forEach(({ uri, action, func }, index) => { -%>
    <%- `${func}${categoryIndex < categories.length - 1 || index < items.length - 1 ? ',' : '' }` %>
<% }) -%>
<% }) -%>
});
