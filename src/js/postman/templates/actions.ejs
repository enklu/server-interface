// AUTOGENERATED CODE: DO NOT EDIT

<% const paths = { %>
    <% item.forEach(({ item, name }) => { %>

    <% }) %>
<% }; %>

const actionNames = {};
const actions = {};
%>

<% export { paths, actionNames, actions }; %>

export const create<%= fileName %>Actions = ({ getify, postify, putify, deletify }) => {
    return {
        <% item.forEach(({ item, name }) => { %>
            // <%= name %>
            <%
                item.forEach(({ item, request: { url, method } }) => {
                    const path = url.path.join('/').replace('{{', ':').replace('}}', '');
                    <%= %>
            %>

            <% }) %>
        <% }) %>
    };
}
